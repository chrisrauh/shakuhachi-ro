---
// Redirect old ?slug= URLs to new /score/[slug] format
const url = new URL(Astro.request.url);
const slug = url.searchParams.get('slug');

if (slug) {
  return Astro.redirect(`/score/${slug}`);
}

// If no slug, redirect to home
return Astro.redirect('/');
---
